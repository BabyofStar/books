# SAM2 微调 Laboro Tomato 数据集报告

## 一、项目背景与目标

**主要目标**：跑通 SAM 2 微调的完整流程，掌握从环境搭建、数据适配、提示工程、模型微调到效果评估的全链路技术。

**次要目标**：在 Laboro Tomato 数据集上验证流程的可行性，观察微调前后的效果变化。

**项目性质**：这是一次**流程学习型项目**，重点在于"走通路径、理解原理、积累经验"，而非追求极致的模型性能。

---

## 二、技术路线总览

```
阶段 1：环境搭建
    ↓
阶段 2：零样本提示分割实验（自动 / 点 / 框 / 框+点）
    ↓ 通过对比理解 SAM2 的能力边界
阶段 3：第一轮微调（矩形 GT + 框+点提示）
    ↓ 跑通微调代码，理解 Mask Decoder 训练机制
阶段 4：伪标签生成（预训练模型预测 → 真实轮廓 GT）
    ↓ 学习伪标签迭代的工程实现
阶段 5：第二轮微调（伪标签 GT + 框提示）
    ↓ 掌握带早停、验证集的完整训练流程
阶段 6：三组对比评估（Pretrained / Round1 / Round2）
    ↓ 学会多模型对比、参数差异分析的评估方法
```

**核心收获**：完整跑通了 SAM2 从零样本推理到领域微调的全流程。

---

## 三、各阶段详情

### 阶段 1：环境配置

| 项目 | 配置 |
|---|---|
| GPU | Kaggle T4 x2 / P100，16 GB 显存 |
| 权重 | `svamitva-sam2-weights/sam2_hiera_large.pt` |
| 配置 | `sam2_hiera_l.yaml`（SAM2 库内置） |
| 数据 | Laboro Tomato：644 train + 161 val |
| 微调范围 | Mask Decoder + Prompt Encoder（冻结 Image Encoder） |

### 阶段 2：零样本提示分割实验

| 提示方式 | 结果 | 学到的知识 |
|---|---|---|
| 自动掩码生成 | 输出大量碎块，无法区分番茄与叶片/茎/背景 | 自动模式无类别意识 |
| 单点提示 | 切出随机几何色块 | 单点提示语义模糊 |
| 框提示 | 切到叶子/茎 | 框内有多个物体时模型不确定 |
| 框 + 中心点 | 稍好但不稳定 | 组合提示优于单一提示 |

**学习要点**：通过对比实验理解了 SAM2 三种预测器的差异，以及提示设计对分割质量的影响。

### 阶段 3：第一轮微调

**配置**：
- GT：YOLO bbox 生成的**矩形掩码**
- 提示：框 + 中心点
- 损失：`seg_loss + score_loss * 0.05`
- 优化器：AdamW, lr=1e-5, weight_decay=4e-5
- 循环上限：10000 步，早停 patience=3

**结果**：

| 指标 | 值 |
|---|---|
| 训练 loss | 0.0041 |
| seg_loss | 0.0031 |
| score_loss | 0.0204 |
| train IoU | 0.8985 |
| 形状规整度 | 0.96（接近矩形） |
| 填充率 | ~1.00 |

**学习要点**：
- 掌握了 SAM2 Mask Decoder 的训练循环结构
- 理解了 `_prep_prompts`、`sam_prompt_encoder`、`sam_mask_decoder` 的调用顺序
- 认识到**训练信号的质量决定模型上限**：矩形 GT 会教出"涂矩形"的模型

### 阶段 4：伪标签生成

**流程**：用预训练 SAM 2 对每个 GT 框推理 → 过滤低质量预测 → 保存真实轮廓伪标签。

**过滤条件**：

```python
MIN_FILL_RATIO = 0.50   # 切漏的丢弃
MAX_FILL_RATIO = 0.95   # 退化成矩形的丢弃
MIN_SCORE      = 0.85   # 模型不自信的丢弃
```

**预训练模型的实际表现**：

| 指标 | 范围 |
|---|---|
| 填充率 | 0.35 ~ 0.76 |
| 形状规整度 | 0.38 ~ 0.79（椭圆特征） |
| score | 0.90 ~ 0.99 |

**学习要点**：
- 掌握了伪标签生成的完整工程实现
- 理解了质量过滤的必要性和阈值设计

### 阶段 5：第二轮微调

**配置**：
- GT：伪标签（真实番茄轮廓）
- 提示：框 + 中心点
- 学习率：5e-6（降低，避免破坏第一轮成果）
- 早停：patience=3

**结果**：

| 指标 | 值 |
|---|---|
| 有效实例 IoU > 0.5 | 82.61% |
| 有效实例 IoU > 0.7 | 82.61% |

**学习要点**：
- 掌握了基于伪标签的完整微调流程
- 理解了**自蒸馏的本质限制**：伪标签来自预训练模型本身时，学生模型上限就是教师模型

### 阶段 6：三组对比评估

| 模型 | 填充率 | 形状规整度 | 解读 |
|---|---|---|---|
| Pretrained | 0.67 | 0.70 | 椭圆轮廓 |
| Round1 | 0.98 | 0.96 | 退化为矩形 |
| Round2 | 0.69 | 0.71 | 椭圆轮廓，≈ Pretrained |

**参数差异分析**：

| 参数 | Round1 vs Round2 差异 | 状态 |
|---|---|---|
| `sam_mask_decoder.iou_token.weight` | 0.0018 | ✅ 已更新 |
| `sam_mask_decoder.iou_prediction_head.layers.0.weight` | 0.0040 | ✅ 已更新 |
| `sam_prompt_encoder.point_embeddings.0.weight` | 0.000000 | ❌ 几乎相同 |
| `sam_mask_decoder.output_hypernetworks_mlps.0.layers.0.weight` | 0.000000 | ❌ 几乎相同 |

**学习要点**：
- 学会了通过参数差异分析定位"训练是否真正生效"
- 理解了**第一轮微调在本项目中是净损失**：把模型带偏后又靠第二轮修回

---

## 四、遇到的主要问题与解决方案

### 4.1 环境配置类

| 问题 | 原因 | 解决方案 |
|---|---|---|
| `MissingConfigException` | Hydra 只认 `pkg://sam2` 路径 | 用库内置配置名 `"sam2_hiera_l.yaml"` |
| `{"model": state_dict}` 包装 | 第三方数据集打包格式 | 加载时检测并解包 |

### 4.2 分割效果类

| 问题 | 原因 | 解决方案 |
|---|---|---|
| 自动分割全是碎块 | 无类别意识 | 改用框/框+点提示 |
| 框提示切到叶子 | 框内多物体 | 加负点 / 用伪标签 |
| IoU 筛选反效果 | 番茄预测 IoU 偏低 | 改用面积 + 位置匹配 |

---

## 五、关键知识点

### 5.1 SAM2 的三个核心预测器

| 预测器 | 核心方法 | 适用场景 |
|---|---|---|
| `SAM2ImagePredictor` | `set_image()` + `predict()` | 单帧图像交互式分割 |
| `SAM2VideoPredictor` | `init_state()` → `propagate_in_video()` | 视频分割与目标追踪 |
| `SAM2AutomaticMaskGenerator` | `generate()` | 全图自动分割 |

**本次项目实际涉及的路径**：

1. **零样本自动分割**：产生碎块，不区分番茄
2. **零样本提示分割**：框+点提示仍会切错
3. **微调 Mask Decoder + Prompt Encoder**：正确的领域适配方向

### 5.2 提示组合艺术

| 提示 | 效果 |
|---|---|
| 单点 | 模糊，容易切错 |
| 框 | 给出范围但不知道切哪个 |
| **框 + 中心点** | **推荐**，准确定位目标 |
| 框 + 多点 + 负点 | 最精确，排除干扰 |

### 5.3 微调策略

- **只微调 Mask Decoder + Prompt Encoder**：参数量小、收敛快
- **冻结 Image Encoder**：ViT 太大，微调会过拟合
- **学习率 1e-5 起步**：避免破坏预训练权重
- **损失**：`seg_loss + score_loss * 0.05`（其中`seg_loss`表示像素级分类损失，`score_loss`表示自评分vs实际IoU差距）

### 5.4 伪标签迭代的本质限制

```
预训练模型输出 → 伪标签 → 训练 → 模型只学会"复现预训练输出"
```

**自蒸馏的上限就是教师模型，无法超越。** 这是第二轮训练与预训练效果几乎相同（形状规整度 0.70 vs 0.71）的根本原因。

**推论**：想真正提升模型能力，必须引入**独立于教师模型的新信息源**。

---

## 六、项目成果与反思

### 6.1 已完成的目标

| 目标 | 完成情况 |
|---|---|
| ✅ 环境搭建 | 掌握 Kaggle + SAM2 的完整配置 |
| ✅ 数据适配 | 独立完成 YOLO bbox → 训练样本的转换 |
| ✅ 零样本推理 | 完整实验了自动/点/框/框+点四种提示方式 |
| ✅ 第一轮微调 | 跑通了 Mask Decoder 训练循环 |
| ✅ 伪标签生成 | 完成了 644 张训练图的伪标签工程 |
| ✅ 第二轮微调 | 掌握带早停、验证集的完整训练流程 |
| ✅ 效果评估 | 学会多模型对比和参数差异分析 |

### 6.2 最终模型表现

| 指标 | 值 |
|---|---|
| 有效实例 IoU > 0.7 比例 | 82.61% |
| 形状规整度 | 0.71（椭圆特征） |
| 填充率 | 0.69（切合轮廓，不涂满框） |

**分布特征**：两极分化
- 82.61% 实例切得非常好（IoU > 0.7）
- 17.39% 实例切得很差（IoU < 0.5）
- 几乎没有中等质量的实例

### 6.3 项目反思

**流程层面（成功）**：
- 完整跑通了 SAM2 微调的全链路
- 积累了显存管理、早停、检查点、混合精度等工程经验
- 建立了"数据加载 → 提示工程 → 微调 → 伪标签迭代 → 评估"的标准流程

**效果层面（有限）**：
- 第二轮 ≈ 预训练，未实现超越
- 根本原因：Laboro Tomato 只有 bbox 标注，伪标签来自预训练模型本身

**关键教训**：
- **训练信号的质量决定模型上限**
- **自蒸馏无法超越教师模型**
- **数据集标注粒度是微调效果的天花板**

---

## 七、后续可以继续探索的方向

虽然本项目已经跑通流程，但如果想进一步提升模型能力，可以考虑：

| 方向 | 说明 | 预期增益 |
|---|---|---|
| **HSV 颜色分割伪标签** | 独立于 SAM2 的信息源 | 高 |
| **多 prompt 一致性伪标签** | 过滤预训练噪声 | 中 |
| **换带 mask 标注数据集** | 根本解决 GT 质量问题 | 高 |
| **人工标注少量精细 mask** | 用少量高质量数据微调 | 高 |
| **在更多数据集上验证流程** | 提高流程的可复用性 | — |

---
## 附录：`SAM2AutomaticMaskGenerator`参数详解与调参指南
| 参数 | 类型 / 默认值 | 含义 | 通用调参建议 |
|---|---|---|---|
| `points_per_side` | int, 32 | 每边采样点数，总点数为 `points_per_side²` | 目标小或密集时**提高**（如 48、64）；显存不足或只需粗略分割时**降低** |
| `points_per_batch` | int, 64 | 模型同时处理的点数，影响速度和显存 | 显存充足时**提高**以加速；出现 OOM 时**降低** |
| `pred_iou_thresh` | float, 0.8 | 基于预测掩码质量的过滤阈值 | 漏检较多时**降低**（如 0.65~0.7）；碎片过多时**提高** |
| `stability_score_thresh` | float, 0.95 | 基于掩码稳定性的过滤阈值 | 边缘复杂或遮挡目标**降低**（如 0.5~0.8）；追求高精度时**提高** |
| `crop_n_layers` | int, 0 | 在裁剪块上再次预测的层数，用于小目标 | 小目标多时**设为 1 或 2**；大目标为主或显存紧张时保持 0 |
| `crop_n_points_downscale_factor` | int, 1 | 裁剪层中减少采样点密度的因子 | 开启裁剪后通常**设为 2**，平衡计算量 |
| `crop_overlap_ratio` | float, ~0.341 | 裁剪块之间的重叠比例 | 目标易被边界切断时**提高**（如 0.4~0.5）；否则保持默认 |
| `crop_nms_thresh` | float, 0.7 | 不同裁剪块之间过滤重复掩码的 NMS 阈值 | 重复掩码多时**降低**；漏检多时**提高** |
| `min_mask_region_area` | int, 0 | 过滤面积小于此值的掩码区域 | 碎片噪声多时**提高**（如 50~500）；小目标多时**保持较低** |
| `box_nms_thresh` | float, 0.7 | 去除重叠掩码框的 NMS 阈值 | 相邻目标被错误抑制时**提高**（如 0.75~0.8）；重叠掩码多时**降低** |
| `mask_threshold` | float, 0.0 | 二值化掩码 logits 的阈值 | 掩码内部有空洞时**提高**（如 0.1~0.3）；边缘过厚时**降低** |
| `output_mode` | str, "binary_mask" | 掩码返回格式：`binary_mask`、`uncompressed_rle`、`coco_rle` | 需要 COCO 格式时改为 `coco_rle`；需要压缩存储时用 `uncompressed_rle`；一般情况保持默认 |

---
*报告完成于 2026-09-13*
